import rightTopLoginButton from '../../../typescript/components/login';
import menuExpand from '../../../typescript/components/menuExpand';

document.addEventListener('DOMContentLoaded', () => {
  rightTopLoginButton();
  menuExpand();
});
